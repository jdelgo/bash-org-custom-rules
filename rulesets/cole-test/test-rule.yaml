name: test-rule
short_description: short desciprtion here
description: This is a long description
category: SECURITY
severity: WARNING
language: JAVASCRIPT
checksum: 7329459e9a538c29dceace92528d61fec64d0882e5412eb16937932311a70fbf
cwe: '121'
arguments: []
tree_sitter_query: (debugger_statement) @stmt
code: |-
  function visit(query, filename, code) {
    const stmt = query.captures["stmt"];
    if (!stmt) return;
    addError(buildError(
      stmt.start.line, stmt.start.col,
      stmt.end.line, stmt.end.col,
      "Remove debugger statement before committing to production.",
      "ERROR",
      "BEST_PRACTICES"
    ));
  }
tests:
- filename: test.js
  code: |-
    function fetchData() {
      debugger;
      return fetch("/api/data");
    }
  annotation_count: 0
is_published: false
